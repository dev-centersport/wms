# Database Configuration (PostgreSQL example)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=mysecretpassword
DB_NAME=wms_db

# TypeORM Settings
DB_SYNCHRONIZE=false  # Auto-create schema (disable in production!)
DB_LOGGING=true     # Show SQL queries in logs

# Application Environment
NODE_ENV=production